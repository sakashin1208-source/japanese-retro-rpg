# 『妖幻奇譚 〜もののけ草子〜』Godot移植版 レビュー依頼書

## 1. 概要
本作は、ブラウザ向け（HTML5 Canvas / TypeScript & JavaScript）として開発されていた和風レトロJRPG『妖幻奇譚 〜もののけ草子〜』を、**Godot Engine 4.7.2（Forward+ / Compatibility）** へ完全移植・再構築したプロジェクトです。
現在、主要コアシステム（データ層、ステート管理、マップ探索・隊列歩行、NPC会話・神社セーブ、コマンドバトル・ターゲット選択、メニューUI、BGM/SE音響）の移行と不具合改修が完了し、統合自動テスト全59件がすべてパスする状態に到達しました。

つきましては、コードベース全体の設計、ロジック整合性、および今後の拡張性について、Claude Code による包括的なコードレビューを依頼いたします。

---

## 2. 開発環境・プロジェクト構成

- **エンジン**: Godot Engine v4.7.2.stable.official.ed1daf0bf
- **解像度**: 1280 × 960（4:3 アスペクト比維持、`canvas_items` ストレッチモード）
- **描画設定**: Nearest テクスチャフィルタ（`default_texture_filter = 0`）、ピクセルパーフェクト描画
- **プロジェクト配置**: `C:\dev\japanese-retro-rpg\godot`

### 主要ディレクトリ構成
```
godot/
├── project.godot               # プロジェクト設定・Autoload・入力マップ定義
├── assets/
│   ├── data/master_data.json   # 不変マスターデータ（主人公3名、技17種、敵59種、NPC17名、全3章）
│   ├── fonts/                  # PixelMplus10 / DotGothic16 フォント
│   ├── portraits/              # 顔ポートレート（29枚）
│   ├── sprites/                # キャラ・魔物スプライト（112枚）
│   └── tiles/                  # マップタイル（25枚）
├── scenes/
│   ├── main/main.tscn          # ブート・解像度初期化
│   ├── title/title_scene.tscn  # タイトル画面（はじめから・つづきから・設定）
│   ├── map/map_scene.tscn      # マップ探索・隊列歩行・対話・エンカウント
│   └── battle/battle_scene.tscn# コマンド戦闘・敵照準選択・結果処理
├── scripts/
│   ├── autoloads/              # GameState, SaveManager, AudioManager, SceneManager
│   ├── data/                   # MasterData.gd（JSONパース、章解放判定）
│   ├── scenes/                 # 各シーンコントローラ（map_scene.gd, battle_scene.gd, title_scene.gd）
│   └── ui/                     # DialogBox, MenuWindow, ItemWindow, StatusWindow, UrushiFrame
└── tests/
    ├── test_scene.tscn         # 統合自動テストスイート（受入全10項目 / 59ケース）
    └── test_runner.gd          # テストランナー本体
```

---

## 3. 実装アーキテクチャ

### (1) データ層・状態管理
- **MasterData (`scripts/data/master_data.gd`)**:
  - `master_data.json` を一括読込し、主人公初期値、技スペック、敵パラメータ・行動AI重み、NPC会話、章ごとのスポーン・セーブ・復活地点（`revive`）をキャッシュ提供。
  - 章のアンロック状態は `is_chapter_unlocked()` により、ボス撃破フラグ・神具所持フラグから動的導出。
- **GameState (`scripts/autoloads/game_state.gd`)**:
  - 実行時の可変状態（所持金、現在章、パーティ3名のHP/MP/Lv/EXP/ステータス、アイテム所持数、ボス撃破辞書、神具辞書、プレイヤー座標 `player_pos`）を一元管理。
  - `reset()` により第1章初期状態へのクリーン復帰を保証。
- **SaveManager (`scripts/autoloads/save_manager.gd`)**:
  - `user://save_game.json` に対するセーブ・ロード・存在判定・クリアを実装。

### (2) マップ探索 ＆ 隊列システム (`scripts/scenes/map_scene.gd`)
- **グリッドベース歩行**: タイルサイズ 64×64、スムーズな線形補間移動（`move_toward`）、障害物・水路判定。
- **3人隊列システム**:
  - 疾風（先頭）、小夜（2番手）、朧（3番手）の歩行履歴スタック管理。
  - キャラ描画は `Sprite2D` ＋ `Polygon2D`（半透明楕円影、`show_behind_parent = true`）ノード構成。
- **NPC・ギミック判定**:
  - 全17名のNPC配置、話しかけ判定、宿屋・回復判定。
  - 白鷺神社の賽銭箱・神鏡インタラクション（HP/MP全快 ＋ セーブ自動実行）。
- **エンカウント**:
  - 歩数カウントダウンによるランダムエンカウント、ボスエリア侵入によるボストリガー。

### (3) コマンドバトルシステム (`scripts/scenes/battle_scene.gd`)
- **王道JRPGレイアウト**:
  - 画面左側: 敵スプライト配置（1〜3体等間隔自動均等整列、ボス192px / 通常128px）。
  - 画面右側: 味方3名のスプライト ＋ 漆調ステータスカード（HP/MP、行動順インジケータ）。
  - 画面下部: メッセージログ（左） ＋ コマンド・ターゲット選択ウィンドウ（右）。
- **ターゲット選択モード**:
  - 単体攻撃・単体技・道具使用時に敵/味方ターゲット選択へシームレス移行。
  - 生存対象のみを抽出して循環選択、対象の頭上に逆三角照準（`▼`）を浮遊アニメーション表示。
  - キーボード（上下左右＋Z決定/X取消）および画面上の敵・味方・リスト直接クリック/タップに対応。
- **確定計算式**:
  - 通常攻撃: `floor(atk * buffAtk * 1.4 - def * buffDef * 0.7)`
  - 物理技: `floor(atk * buffAtk * 1.4 * power - def * buffDef * 0.5)`
  - 魔法技: `floor(matk * 1.8 * power - def * buffDef * 0.4)`
  - 回復技: `power + matk * 1.2`
- **行動順序（SPD厳密ソート）**:
  - 味方・敵全アクターの `spd * buffSpd` を毎ターン厳密ソートして行動再生。
- **勝敗・全滅復帰**:
  - 勝利時: EXP・両獲得、レベルアップ処理、ボス撃破フラグ更新、エンカウント地点への復帰。
  - 全滅敗北時: HP/MP全快の上で各章の復活地点（白鷺神社: `x=36, y=8`）へ送還。

### (4) UI・対話システム
- **DialogBox (`scripts/ui/dialog_box.gd`)**:
  - 漆枠ウィンドウ、顔ポートレート（NPC別エイリアスマッピング対応）、タイプライター文字送り。
  - 決定キー（Z/Enter/Space）やクリック/タップで「文字早送り → 次ページ進行 → 会話終了・探索復帰」を完全制御。
- **各種ウィンドウ**:
  - `MenuWindow`（絵巻物手鑑: 強さ・道具・章移動・記録・閉じる）
  - `ItemWindow`（道具一覧・効果説明・マップ/戦闘使用）
  - `StatusWindow`（キャラ詳細能力値・習得技一覧）

---

## 4. 直近で実施した主要バグ改修内容

| 項目 | 発生していた問題 | 根本原因 | 改修内容 |
| :--- | :--- | :--- | :--- |
| **① キャラ描画不具合** | マップ上でキャラやNPCが白い四角で表示される | 毎フレームの `draw_texture` 直描きによるテクスチャバインド破綻、およびNPCスプライト名の不一致 | `Sprite2D` ＋ 影ポリゴンのシーンノード構成へ移行。スプライト名の後方互換エイリアスマッピングを追加。 |
| **② 戦闘後の座標リセット** | 敵を倒したあと初期地点 `(12, 14)` に戻ってしまう | 移動時にローカル座標のみ更新され、グローバルな `GameState.player_pos` への同期が漏れていた | 歩行完了時および戦闘遷移直前に `GameState.player_pos` を同期。全滅時は各章の `revive` 座標へ送還。 |
| **③ 敵ターゲット未選択** | 攻撃時に敵を選べず、常に先頭の敵に強制決定される | ターゲット選択ステートが存在しなかった | ターゲット選択モード（カーソル循環・照準 `▼` 表示・直接クリック対応・敵3体整列）を実装。 |
| **④ 会話進行不能（詰み）** | 村人に話しかけた後、決定を押してもウィンドウが消えない | `map_scene.gd` の `_unhandled_input` がUIオープン時に即 `return` し、入力を横流ししていなかった | 最前面UIへ入力をルーティング。Z/Enter/Space/クリックの多重バインドとエコー抑止を実装。 |

---

## 5. テスト・検証実績

- **自動統合テスト (`res://tests/test_scene.tscn`)**: **全59件 全件合格（ALL TESTS PASSED）**
  - M1: 基盤設定（1280x960, Nearestフィルタ）
  - M2: マスターデータ整合性（主人公3名、技17種、敵59種、NPC17名、全3章）
  - M3: アセットファイル存在確認（スプライト112枚、タイル25枚、ポートレート29枚）
  - 受入1〜7: 戦闘勝利EXP反映、全滅回復、リセット初期化、セーブ＆ロード完全一致、マップ/戦闘アイテム消費、素早さ行動順ソート、技ダメージ期待値
  - 受入8: 複数敵ターゲット選択・確定キューイング・撃破除外判定
  - 受入9: 勝利/逃走時のエンカウント座標保持、全滅時の白鷺神社送還
  - 受入10: 村人会話開始・早送り・ページ送り・決定での完全クローズ・探索復帰
- **実機描画検証**:
  - 3人隊列歩行、敵3体出現時の重なり解消レイアウト、頭上照準 `▼` の描画キャプチャを目視検証済み。

---

## 6. Claude Code への重点レビュー依頼項目

1. **アーキテクチャ ＆ メモリ管理**:
   - シーン遷移（`SceneManager`）やUI開閉時におけるノード解放（`queue_free`）やシグナル接続のライフサイクルにメモリリークの懸念がないか。
   - （Godot終了時に報告される ObjectDB / Resource の残存警告に対する予防的改善点）
2. **Web版（元仕様）とのロジック整合性**:
   - `MasterData` やダメージ計算式、敵AIの重み付き抽選（`actions[].rate`）において、エッジケース（防御力過多によるゼロ除算やマイナスダメージ等）のガードが十分か。
3. **入力系の堅牢性**:
   - PCキーボード、マウス操作、将来のスマートフォン（タッチ/バーチャルパッド）やゲームパッドへの展開を見据えた入力抽象化の適切さ。
4. **コード品質 ＆ 保守性**:
   - GDScript 2.0（Godot 4）の静的型付け（Static Typing）の徹底度、リファクタリング推奨箇所の有無。